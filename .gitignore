node_modules/
# rendered HTML dumps from `npm run check:html`
.render/

# packaged theme artifact (rebuild with the zip command in README)
zazacheats-theme.zip

# packaged Shopify theme artifact (rebuild with resellplug-theme/tools/package.sh)
resellplug-theme.zip
